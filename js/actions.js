var ACTIONS = {
	func1 : function() {
		CREATE_ELEMENTS.CLEAR();
		MAIN.CREATE();
		CREATE_ELEMENTS.SET_MENU(0);
	},
	func2 : function() {
		CREATE_ELEMENTS.CLEAR();
		SALE.CREATE();
		CREATE_ELEMENTS.SET_MENU(1);
	},
	func3 : function() {
		CREATE_ELEMENTS.CLEAR();
		ORDER.CREATE();
		CREATE_ELEMENTS.SET_MENU(2);
	},
	func4 : function() {
		CREATE_ELEMENTS.CLEAR();
		CREATE_ELEMENTS.SET_MENU(3);
	},
	func5 : function() {
		CREATE_ELEMENTS.CLEAR();
		CREATE_ELEMENTS.SET_MENU(4);
	},
}